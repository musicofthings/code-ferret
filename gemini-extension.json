{
  "name": "code-ferret",
  "version": "0.4.0",
  "description": "Diff-scoped semantic code review: bug hunting across logic, security, concurrency, performance, and API-contract vectors.",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "code-ferret": {
      "command": "node",
      "args": ["${extensionPath}${/}mcp-server${/}server${/}index.js"],
      "cwd": "${extensionPath}",
      "env": {
        "CODEFERRET_ROOT": "${extensionPath}"
      }
    }
  }
}
